<h1>Edit Topic</h1>
<div class="row">
  <div class="col-md-4">
    <p>Guidelines for topics</p>
    <ul>
      <li>Make sure the it rhymes.</li>
      <li>Don't use the letter "A".</li>
      <li>The incessant use of hashtags will get you banned.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @topic do |f| %>
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :description, class: 'form-control', placeholder: "Enter topic title" %>
      </div>
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_area :description, rows: 8, class: 'form-control', placeholder: "Enter topic body" %>
      </div>

      <div class="form-group">
      <%= f.label :public, class: 'checkbox' do %>
        <%= f.check_box :public %> Public topic
      <% end %>
      </div>

      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>